<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ng.com.iqubesolutions.drinkshub">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="ng.com.iqubesolutions.drinkshub.app.DrinksHub"
        android:theme="@style/AppTheme">

        <activity
            android:name="ng.com.iqubesolutions.drinkshub.MainActivity"
            android:label="@string/app_name" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name="ng.com.iqubesolutions.drinkshub.LoginActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ng.com.iqubesolutions.drinkshub.MainActivity" />
            </activity>

    </application>

</manifest>